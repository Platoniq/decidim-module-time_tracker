<div class="card__grid milestone-card">
  <div class="card__grid-img">
    <%= image %>
  </div>
  <div class="card__grid-text">

    <%= title %>

    <div class="card__text">

        <% if list? %>
            <b><%= model.title %></b>
        <% end %>

        <div class="text-ellipsis">
          <%= model.description %>
        </div>
    </div>

    <div class="card__grid-metadata">
      <% if list? %>
        <div class="text"> <%= t(".time_dedicated") %></div>
        <div class="text">
          <strong><%= clockify_seconds(seconds_elapsed) %></strong>
        </div>
      <% else %>
        <div class="text">
          <%= l model.created_at.to_date, format: :decidim_short_with_month_name_short %>
        </div>
        <div class="text">
          <%= l model.created_at, format: :time_of_day %>
        </div>
      <% end %>
    </div>
  </div>
</div>
